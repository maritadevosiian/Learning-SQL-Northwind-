# Northwind SQL Analysis Project

Hi, I'm **maritadevosiian**, an Information Security student. 
This repository contains a collection of SQL queries developed during my analysis of the Northwind database.

### Key Skills Demonstrated:
* **Relational Database Design:** Understanding table relationships and schema mapping.
* **Joins:** Proficient use of `INNER JOIN` and `LEFT JOIN` to combine data from multiple sources.
* **Aggregations & Grouping:** Using `GROUP BY`, `COUNT`, and `SUM` for data summarization.
* **Advanced Filtering:** Applying logic with `WHERE` and `HAVING` clauses.
* **Complex Queries:** Experience with **Self-Joins** and **Multi-table Joins** (connecting up to 5 tables in a single query).

### Why this project?
As a security student, I believe that mastering SQL is essential for understanding data integrity and protecting databases against common vulnerabilities like SQL Injection.
